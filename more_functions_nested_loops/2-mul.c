#include "main.h"

/**
 *mul - function that multiplies two integer
 *@a: first variable to multiply
 *@b: second variable to multiply
 *Return: a * b  (Succes)
 */
int mul(int a, int b)
{
return (a * b);
}
