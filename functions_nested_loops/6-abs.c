#include "main.h"
/**
 * _abs - prints the absolute value of a number
 * @i: Number target
 *Return: Always 0 (Succes)
 */
int _abs(int i)
{
int n = _abs(i);
if (i > 0)
_putchar(n);
else if (i < 0)
_putchar(n);
else
_putchar(n);
return (0);
}
