#include "main.h"
/**
 * _abs - prints the absolute value of a number
 * @i: Number target
 *Return: Always 0 (Succes)
 */
int _abs(int i)
{
i = _abs(i);
if (i > 0)
{
_putchar(i);
}
else if (i < 0)
{
_putchar(i);
}
else
{
_putchar(i);
}
return (0);
}
